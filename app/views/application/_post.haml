.blog-post
  %h2.blog-post-title
    = link_to post.title, post

  %p.blog-post-meta
    = post.created_at.strftime('%Y-%m-%d %H:%M')
    by
    = post.pretty_author

  .wrapper.mb60
    .inline.pull-left
      = rating(post)
    .inline.pull-right
      = visits_count(post)

  = post.body.gsub("\n", '<br />').html_safe
